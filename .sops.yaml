# sops config: encrypt secrets with the age key whose private half lives at
# ~/.config/sops/age/keys.txt (BACK THAT KEY UP in your password manager —
# without it .env.sops cannot be decrypted).
creation_rules:
  - path_regex: (^|/)\.env$
    age: age16m09lxysn6j7tsvlhmtn8mnw797fu457d9vrn7tt6vjklrt0jvcs8s4ssq
