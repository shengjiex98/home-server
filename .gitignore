# Secrets — never commit the plaintext (.env.sops and hosts/*.sops.env, the
# encrypted copies, ARE committed)
.env
# Which host's config this server uses (machine-local)
.host

# homepage writes logs into its bind-mounted config dir
gateway/homepage/logs/

# Local editor / OS cruft
.DS_Store
*.swp
