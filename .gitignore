# Secrets — never commit the plaintext (.env.sops, the encrypted copy, IS committed)
.env

# homepage writes logs into its bind-mounted config dir
gateway/homepage/logs/

# Local editor / OS cruft
.DS_Store
*.swp
